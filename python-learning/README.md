# Python Learning

A collection of my Python projects while learning through small, practical projects.

## Projects

1. Calculator
2. Age Calculator
3. Number Guessing Game
4. Password Generator
5. Weather App
6. Crypto Price Checker
7. Crypto Price Tracker
8. Expense Tracker
9. Pandas Expense Tracker

## What I've learned

- Variables
- `input()` and `print()`
- Arithmetic
- `if` / `elif` / `else`
- `while` loops
- `for` loops
- Functions
- Parameters and `return`
- Lists
- Dictionaries
- Random numbers
- APIs and HTTP requests
- JSON responses
- Nested data
- Basic Pandas

## Important note

Some project files are reconstructed from my previous work and notes because the original files were lost. They preserve the beginner-level approach and concepts I had learned rather than pretending to be exact copies of every original line.

## API key security

Never put a real API key in GitHub.

For the CoinMarketCap projects, set the `CMC_API_KEY` environment variable locally.

## Dependencies

```bash
pip install requests keyboard python-weather pandas
```
