# Pandas Expense Tracker

The Pandas version of the expense tracker.

It converts a list of expense dictionaries into a DataFrame and uses Pandas methods to calculate values.

## Concepts practiced

- Pandas
- DataFrames
- Selecting a column
- `.mean()`
- `.sum()`
- `.max()`
- `.idxmax()`
- `.loc[]`
