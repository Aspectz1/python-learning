# Weather App

A weather application using the `python-weather` library.

## Concepts practiced

- Importing libraries
- `async`
- `await`
- `asyncio`
- Objects and properties
- User choices
- `if` / `elif` / `else`

The original project used properties such as:

- `weather.temperature`
- `weather.humidity`
- `weather.wind_speed`
